const fs = require('fs');
const fsex = require('fs-extra');

const FileReader={
    /**
     * @description Read a file as an Array
     * @param path
     * @param callback
     */
    readeFile:function (path,callback) {
        //check if file exists
        fs.stat(path,function (err,stats) {

        })
    }
};